# Local build artefacts / secrets never belong in the repo
*.log
*.local
secrets.env
config.env
node_modules/
dashboard/node_modules/
# Commercial thinking lives in ../genkan-commercial, never here
COMMERCIAL*
commercial/
# Backups deploy.sh writes beside the real files carry the same real values.
config.env.*
!config.env.example
secrets.env.*
!secrets.env.example
