[Unit]
Description=Genkan per-service accounting (learn service IPs, count bytes per kid)
After=docker.service
[Service]
Type=oneshot
EnvironmentFile=/srv/projects/internal/kids-network/secrets.env
ExecStart=/bin/sh -c '/usr/local/bin/genkan-servicemap; /usr/local/bin/genkan-servicemeter'
