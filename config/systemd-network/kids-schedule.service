# Installed to /etc/systemd/system/ by deploy.sh.
[Unit]
Description=Genkan scheduled bedtimes (apply and lift)
After=docker.service
[Service]
Type=oneshot
EnvironmentFile=/srv/projects/internal/kids-network/secrets.env
ExecStart=/usr/local/bin/genkan-schedule apply
