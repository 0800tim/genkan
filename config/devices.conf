# Kid device map for `kidnet`. Format:  <kid>=<ip>[,<ip>...]
# Use DHCP reservations so each device has a stable .60.x, then list here.
# Groups can share nothing or overlap; `kidnet off all` blocks every listed IP.
#
# Filled in once devices join the island (check leases: `kidnet leases`).
#
# Ben=192.168.60.101,192.168.60.102
# Cleo=192.168.60.111
